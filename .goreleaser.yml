version: 2

project_name: interchange

before:
  hooks:
    - go mod tidy

builds:
  - id: interchange
    main: ./cmd/interchange
    binary: interchange
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/CarriedWorldUniverse/interchange/internal/version.Version={{.Version}}

archives:
  - id: default
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format: tar.gz
    files:
      - README*
      - LICENSE*

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: '{{ incpatch .Version }}-next'

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - Merge pull request
      - Merge branch

release:
  github:
    owner: CarriedWorldUniverse
    name: interchange
  draft: false
  prerelease: auto
  name_template: '{{ .ProjectName }} {{ .Tag }}'
